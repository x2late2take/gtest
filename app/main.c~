#include "tribonacchi.h"

int main() {
    int n;
    printf("Введите число n: ");
    scanf("%d", &n);

    unsigned long long result = tribonacci(n);
    printf("Tribonacchi(%d) = %llu\n", n, result);

    return 0;
}
